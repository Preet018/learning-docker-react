sudo: required
services:
  - docker

before_install:
  - docker build -t preet1018/learning-docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true preet1018/learning-docker-react npm test
